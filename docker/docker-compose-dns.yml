name: tomcat
services:
  nginx:
    image: nginx
    ports:
      - 1080:80
    volumes:
      - ../nginx/default-single.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on: [tomcat0]
  tomcat0:
    image: jtips/tomcat:dns
    environment:
      DNS_MEMBERSHIP_SERVICE_NAME: tomcat0
    deploy:
      replicas: 3
      resources:
        limits:
          memory: 256M
          cpus: 2
